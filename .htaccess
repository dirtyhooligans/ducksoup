<IfModule mod_rewrite.c>
    RewriteEngine on
	
	# RewriteCond %{REQUEST_URI} ^(.*)(app)\/(.*)$
	# RewriteRule ^(app)/(.*)$ applications/$2 [L]

	RewriteRule ^$   public/    [L]
	RewriteRule (.*) public/$1  [L]

</IfModule>
